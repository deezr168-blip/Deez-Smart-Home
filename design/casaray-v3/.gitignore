# Renders are regenerated by tools/test_and_capture.cjs. They are not
# committed: scripts/ha_validate.sh section 7 allows PNGs only in
# docs/mockups/ (owner renders) and artifacts/screenshots/ (live captures),
# and a prototype render is neither.
screenshots/*.png
